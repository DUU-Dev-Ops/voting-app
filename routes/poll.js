var express = require('express');
var router = express.Router();

router.post('/api/launch', function(req, res, next) {
    // Save the post data into database as a poll.
});

module.exports = router;
